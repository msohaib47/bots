# Apify API token (required) — sign up at https://apify.com, get your token
# from Settings > Integrations > API tokens
APIFY_API_TOKEN=

# Email configuration (optional — if not set, email reports are skipped)

# Gmail: Get an App Password from https://myaccount.google.com/apppasswords
# Other providers: Use your SMTP credentials
SMTP_USER=solutionzeroone@gmail.com
SMTP_PASS=tgqk kwqr blao pdcl

# SMTP server and port (defaults below are for Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Recipient email (defaults to solutionzeroone@gmail.com)
EMAIL_RECIPIENT=solutionzeroone@gmail.com

# Optional: Database path (defaults to /home/claude/data/ebay_sold.db)
# EBAY_DB_PATH=/home/claude/data/ebay_sold.db
